import * as React from 'react';
import AppContainer from './container/App';

class App extends React.Component {
  public render() {
    return (
      <AppContainer />
    );
  }
}

export default App;
